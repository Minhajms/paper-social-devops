version: '3'

services:
  web:
    build: .
    ports:
      - "80:3000"
    restart: always
    environment:
      - CLOUD_PROVIDER={{ cloud_provider }}
      - INSTANCE_ID={{ instance_id }}
      - REGION={{ region }}
